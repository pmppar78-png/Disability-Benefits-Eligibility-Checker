[build]
  publish = "."
  functions = "netlify/functions"

# Enforce a single canonical URL form: extensionless, https, apex domain.
# pretty_urls = true makes Netlify 301 any `.html` request to its
# extensionless equivalent and serve the extensionless URL with a 200.
[build.processing.html]
  pretty_urls = true
